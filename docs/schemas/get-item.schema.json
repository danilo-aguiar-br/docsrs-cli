{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "docsrs-cli get-item success data",
  "type": "object",
  "description": "Payload under envelope.data for command get-item. Outer envelope always has schema_version=1, ok, command, duration_ms. Optional fields are omitted when absent (never JSON null).",
  "required": [
    "crate_name",
    "item_type",
    "item_path",
    "item_name",
    "version",
    "markdown",
    "empty",
    "truncated",
    "source_url",
    "title",
    "cache_hit"
  ],
  "properties": {
    "crate_name": {
      "type": "string"
    },
    "item_type": {
      "type": "string"
    },
    "item_path": {
      "type": "string"
    },
    "item_name": {
      "type": "string"
    },
    "version": {
      "type": "string"
    },
    "resolved_version": {
      "type": "string",
      "description": "Omitted when docs.rs did not resolve a concrete version (never JSON null)"
    },
    "markdown": {
      "type": "string"
    },
    "empty": {
      "type": "boolean"
    },
    "truncated": {
      "type": "boolean"
    },
    "source_url": {
      "type": "string",
      "format": "uri"
    },
    "title": {
      "type": "string"
    },
    "cache_hit": {
      "type": "boolean",
      "description": "True when the HTTP response body was served from the local disk cache"
    },
    "extraction": {
      "type": "string",
      "enum": ["method", "item_page"],
      "description": "Present for associated-method success: always method when the anchor was found (1.2.0+ fail-closed). item_page is legacy and MUST NOT appear on success — missing anchors return not_found exit 66. Omitted for non-method items."
    },
    "resolved_item_path": {
      "type": "string",
      "description": "Canonical rustdoc path when a root/reexport path was resolved via all.html. Omitted when the requested path was used as-is (never JSON null)."
    }
  },
  "additionalProperties": false
}
