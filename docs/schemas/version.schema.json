{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "docsrs-cli version success data",
  "type": "object",
  "description": "Payload under envelope.data for command version. Outer envelope always has schema_version=1, ok, command, duration_ms.",
  "required": [
    "name",
    "version",
    "msrv",
    "os",
    "arch"
  ],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "const": "docsrs-cli"
    },
    "version": {
      "type": "string"
    },
    "msrv": {
      "type": "string"
    },
    "os": {
      "type": "string",
      "description": "Target family the binary was built for. Always present: the field is a compile-time constant, never an option."
    },
    "arch": {
      "type": "string",
      "description": "Target architecture the binary was built for. Always present, for the same reason as os."
    }
  }
}
